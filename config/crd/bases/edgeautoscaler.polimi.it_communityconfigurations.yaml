
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.3.0
  creationTimestamp: null
  name: communityconfigurations.edgeautoscaler.polimi.it
spec:
  group: edgeautoscaler.polimi.it
  names:
    kind: CommunityConfiguration
    listKind: CommunityConfigurationList
    plural: communityconfigurations
    singular: communityconfiguration
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CommunityConfiguration is a configuration for the autoscaling
          system.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CommunityConfigurationSpec is the spec for a CommunityConfiguration
              CRD
            properties:
              community-size:
                description: CommunitySize is the upper bound on the community size
                format: int64
                type: integer
              iterations:
                default: 20
                description: Iterations specifies how many times the speaking process
                  occurs in SLPA
                format: int64
                type: integer
              maximum-delay:
                description: MaximumDelay is the upper bound for the delay inside
                  a community measured in milliseconds
                format: int32
                maximum: 100
                minimum: 0
                type: integer
              probability-threshold:
                default: 30
                description: ProbabilityThreshold is the lower bound to accept labels
                  in SLPA algorithm. It is a percentage
                format: int32
                maximum: 100
                minimum: 0
                type: integer
              slpa-service:
                default: slpa-rest
                description: SlpaService is the service:port offering the SLPA algorithm
                type: string
            type: object
          status:
            description: CommunityConfigurationStatus represents the status of the
              community configurations.
            properties:
              generated-communities:
                description: Communities represents the community generated by slpa
                  for a given namespace
                items:
                  type: string
                type: array
            required:
            - generated-communities
            type: object
        required:
        - spec
        - status
        type: object
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []

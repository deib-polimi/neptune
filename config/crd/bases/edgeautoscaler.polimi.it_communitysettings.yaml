
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.3.0
  creationTimestamp: null
  name: communitysettings.edgeautoscaler.polimi.it
spec:
  group: edgeautoscaler.polimi.it
  names:
    kind: CommunitySettings
    listKind: CommunitySettingsList
    plural: communitysettings
    singular: communitysettings
  scope: Cluster
  validation:
    openAPIV3Schema:
      description: CommunitySettings is a configuration for the autoscaling system.
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          properties:
            community-size:
              format: int64
              type: integer
            iterations:
              default: 20
              format: int64
              type: integer
            maximum-delay:
              format: int32
              maximum: 100
              minimum: 0
              type: integer
            probability-threshold:
              default: 30
              format: int32
              maximum: 100
              minimum: 0
              type: integer
          type: object
      required:
      - spec
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
